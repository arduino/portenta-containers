# Arduino Portenta X8 Out Of the Box Application

WIP

### Changing Web App VITE_ARDUINO_CLOUD_URL
URLs to extarnal application / websites can be easily changed by editing the `webapp/.env.prod` file. If you started the web app via `npm run start`, you can use the `webapp/.env.prod`.